# Clarity — Environment Variables
# Copy to .env.local and fill in values, or use: npm run env:inject

# App
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Turso (LibSQL)
TURSO_DATABASE_URL=libsql://clarity-aventerica89.aws-us-west-2.turso.io
TURSO_AUTH_TOKEN=

# Token encryption — generate with: openssl rand -base64 32
TOKEN_ENCRYPTION_KEY=

# Better Auth — generate with: openssl rand -base64 32
BETTER_AUTH_SECRET=
BETTER_AUTH_URL=http://localhost:3000

# Google OAuth (Cloud Console → APIs & Services → Credentials)
# Required scopes: openid, profile, email, calendar.readonly, gmail.readonly
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# Anthropic — uses Claude.ai OAuth (no API key needed)
# See CLAUDE.md for the createClient() pattern

# Vercel Cron — generate with: openssl rand -hex 16
CRON_SECRET=

# Plaid (console.plaid.com → Team Settings → Keys)
PLAID_CLIENT_ID=
PLAID_SECRET=
PLAID_ENV=sandbox

# Todoist OAuth (developer.todoist.com → App Management → Create app)
TODOIST_CLIENT_ID=
TODOIST_CLIENT_SECRET=

# Upstash Redis — rate limiting (console.upstash.com → database → REST API tab)
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=
